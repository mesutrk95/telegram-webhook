{
  "name": "telegram-webhooks-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:api": "node api/index.js",
    "start:ui": "yarn --cwd ./ui dev -- --port 3222",
    "build:ui": "cross-env EXPORT_UI_DIR=../dist yarn --cwd ./ui build",
    "postinstall": "(cd api && echo 'In ./api directory' && yarn install) && (cd ../ui && echo 'In ./ui directory' && yarn install)"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3" 
  }
}
